# Base URL of the deployed Functions app (Milestone 4). Leave UNSET for local dev
# (Vite proxies /api -> http://localhost:7071). In production set this at build
# time to the Function App URL from Terraform output `function_app_base_url`, e.g.:
# VITE_API_BASE_URL=https://guildlive-func-ab12cd.azurewebsites.net
